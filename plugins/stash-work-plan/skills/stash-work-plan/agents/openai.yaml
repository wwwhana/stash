interface:
  display_name: "Stash Work Plan"
  short_description: "Resume and verify shared Stash work"
  default_prompt: "Use $stash-work-plan to resume this project's next available work item and continue from its checkpoint."
dependencies:
  tools:
    - type: "mcp"
      value: "stash"
      description: "Configured Stash MCP server with resumable work tools"
policy:
  allow_implicit_invocation: true
