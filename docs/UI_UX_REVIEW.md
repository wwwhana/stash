# UI/UX와 기억·목표·작업 연결 검토

검토일: 2026-09-05. 추가 수정: 2026-09-08. Orca의 실제 서버 화면에서 발견한 문제를 현재 저장소에서 수정했다. 수정 후에는 별도 로컬 서버와 테스트용 PostgreSQL에서 저장·조회하고, Orca에서 데스크톱과 393px 모바일 화면을 조작했다. 운영 서버에는 아직 반영하지 않았다.

## 화면에서 확인한 문제와 수정

| 문제 | 수정한 동작 |
| --- | --- |
| 로그인 전 작업 화면과 인증 오류가 함께 보임 | 인증 상태를 먼저 확인하고 로그인 화면을 표시한다. 로그인 후 이전 화면 주소로 돌아온다. |
| 긴 계정 표시와 반복 요약이 내용을 밀어냄 | 계정 메뉴를 접고, 작업 공간 선택을 사이드바로 옮겼다. 중복 요약·비율·설명을 줄였다. |
| 지도가 첫 화면을 차지하고 항목을 읽기 어려움 | 목록을 기본으로 표시한다. 목록·지도 전환과 항목 유형별 선택을 분리했다. |
| 모바일 메뉴가 잘림 | 화면 선택 상자를 제공하고 목록·상세·필터 배치를 좁은 화면에 맞췄다. |
| 선택 상세를 찾기 어렵고 키보드 이동이 끊김 | 상세로 초점을 옮기고, 닫을 때 선택 버튼으로 돌아온다. Esc로 닫을 수 있다. |
| 빈 보드 칸이 반복됨 | 항목이 있는 상태를 표시한다. 작업이 없을 때는 빈 목록 안내 하나를 표시한다. |
| 사실·가설 메뉴만 있어 경험과 실패 기록을 찾기 어려움 | 기억 메뉴에서 네 가지 유형을 함께 조회하고 유형·내용으로 검색한다. 개요에는 ‘연결된 기억’이라고 표시한다. |
| 기억 내용이 256자에서 끊김 | 상세에서 원문 전체를 나누어 읽는다. 실패의 이유·교훈과 가설의 확인 방법도 조회한다. |
| 큰 프로젝트가 빈 화면처럼 표시됨 | 지도 응답을 크기 제한 안에서 나누어 읽는다. 조회 실패와 실제 빈 목록을 구별한다. |
| 공간을 빠르게 바꾸면 이전 결과가 덮어씀 | 현재 요청의 결과만 화면에 반영한다. 뒤로 가기와 선택 항목 주소도 복원한다. |
| 관리 메뉴가 안내만 표시함 | 에이전트 규칙 복사와 검색 데이터 관리의 조회·다시 시도·다시 만들기를 기존 기능에 연결했다. |
| 상단의 테마 선택이 무엇을 바꾸는지 불분명함 | 사이드바 아래에 ‘화면 모드’라는 이름을 붙이고 기기 설정·라이트 모드·다크 모드를 표시한다. 언어 선택도 함께 배치했다. |
| 화면 문구가 한국어에 고정됨 | 메뉴·상세·오류·처리 결과·에이전트 규칙을 한국어와 영어로 분리했다. 언어를 바꿔도 현재 선택과 원문을 유지한다. |
| ‘작업 연결’이 화면의 의미를 잘 전달하지 못함 | ‘작업 흐름’으로 바꿨다. |
| 규칙·검색 관리에서 새로고침하면 작업 공간이 바뀜 | 두 화면의 주소에도 선택한 작업 공간을 유지한다. |
| 저장된 작업 상태가 화면에 바로 반영되지 않음 | 화면을 보고 있을 때 15초마다 갱신한다. 선택·검색 조건·스크롤은 유지한다. |
| 확인할 작업을 여러 화면에서 찾아야 함 | 개요에 ‘확인할 일’을 표시하고 해당 작업·목표를 바로 연다. 서버가 기존 기록에서 계산한다. |
| 작업 상세에서 목적과 담당 범위를 파악하기 어려움 | 작업별 목적·담당 범위·다음 할 일과 관련 기억을 함께 표시한다. 상위 작업 링크는 한 번만 표시한다. |
| 기억 목록도 전체 연결 정보를 기다림 | 목록을 먼저 표시하고 선택할 때 연결 정보를 읽는다. 선택한 항목의 주소를 새로고침해도 복원한다. |
| 빈 Git 화면에서 다음 행동이 없음 | ‘작업 폴더 등록’을 제공한다. 저장소·폴더·선택 브랜치를 저장한 뒤 목록을 다시 읽는다. |

View는 배치·입력·초점 이동을 담당하고, ViewModel은 상태·조회·선택을 담당한다. 문구는 별도 번역 파일에서 관리한다. 기존 API·주소·지도 배치 코드를 재사용했다. 새 라이브러리는 추가하지 않았다.

## 백엔드에서 자동으로 처리하는 부분

- **작업 결과 기록:** 기존 완료·인계·연결 만료 시 경험을 남기는 흐름을 유지한다. 완료 요청을 다시 보내도 같은 결과가 중복 기록되지 않는지 확인했다.
- **사실의 출처:** `fact_sources`에 있는 사실→원본 경험 관계를 작업 지도와 작업 재개 정보까지 이어 준다. 이전에 저장한 사실에도 적용된다. 티켓·목표가 없는 사실도 상세에서 원본 경험을 열 수 있다.
- **직접 지정한 연결 유지:** 에이전트가 직접 연결한 기록을 우선한다. 자동으로 계산한 연결은 별도로 표시하며, 다른 티켓의 기억을 프로젝트 전체에 복사하지 않는다. 만료된 사실은 작업 재개 정보에서 제외한다.
- **작업 묶음의 진행 상황:** 하위 세부 작업에서 완료 수와 표시 상태를 계산한다. 세부 작업이 끝났는데 묶음은 ‘준비’로 남는 표시 문제를 해결했다. 이후 작업이 추가되면 진행 중으로 다시 표시한다. 취소한 작업과 실행할 세부 작업이 없는 묶음도 구별한다.
- **목표 처리:** 세부 작업의 확인 조건과 근거를 검사한 뒤 목표 완료로 이어지는 기존 흐름을 유지한다. 표시용 진행 상태를 계산하면서 저장된 목표 상태나 사용자가 남긴 설명을 덮어쓰지 않는다.
- **큰 응답:** 지도와 기억 원문은 이어 읽을 수 있다. 읽는 도중 내용이 바뀌면 서로 다른 시점의 내용을 합치지 않도록 다시 조회한다.
- **검색 준비와 원문 조회 분리:** 검색용 데이터가 아직 없거나 생성에 실패해도 저장된 원문은 조회할 수 있다. 실제 HTTP 확인에서 발견한 경험·사실 조회 오류를 수정했다.
- **확인할 일:** 막힌 작업·검토 대기·에이전트 연결 만료와 목표 완료 확인·상태 불일치를 저장된 기록에서 계산한다. 에이전트가 별도 목록을 관리할 필요가 없다. 하위 작업의 상태를 요약하는 묶음은 같은 알림을 반복하지 않는다.
- **작업 상세 조회:** 선택한 작업 공간을 확인한 뒤 작업별 목적과 담당 범위를 읽는다. 조회가 만료 처리나 작업 시작으로 이어지지 않는다.
- **기억 정리 상태:** 남은 처리 분량을 계산하는 SQL에 작업 공간 인수가 빠진 오류를 수정했다.

## 티켓과 역할을 다루는 기준

1. 티켓 없이 작업 공간에 기억만 저장하거나 독립 목표와 기억을 연결할 수 있다. 조회 때문에 티켓을 자동 생성하지 않는다.
2. 같은 프로젝트 안에서도 티켓의 담당·작업 설명·필요한 작업 능력·연결 기억을 각각 유지한다. 구현 티켓과 검토 티켓의 재개 정보가 섞이지 않는지 확인했다.
3. 자동 연결은 저장된 출처와 명시된 관계에 근거한다. 프로젝트가 같다는 이유만으로 담당 역할이나 연결을 추측하지 않는다.
4. 작업 능력 표시는 작업을 배정하기 위한 정보다. 접근 권한을 부여하는 설정으로 바꾸지 않았다. 다른 작업 공간의 원문과 출처 조회는 거부한다.
5. 공통 목표를 지정한 프로젝트의 작업 목표 연결 규칙은 유지한다. 공통 목표를 선택하지 않아도 독립 목표들을 개요에서 볼 수 있다.

## 확인 결과

| 경로 | 관찰된 결과 |
| --- | --- |
| 화면 자동 검사 | `node --test internal/web/*.test.cjs` 144개 통과. |
| Go 전체 검사 | `GOCACHE=/tmp/stash-go-cache go test ./...` 통과. |
| 실제 데이터베이스 검사 | 테스트용 pgvector PostgreSQL을 지정한 `go test ./internal/brain -count=1` 통과. 자동 결과, 중복 완료, 출처 연결, 직접 연결 우선, 만료 사실 제외, 진행 상황 갱신, 티켓 없는 기억, 역할별 기억 분리를 확인했다. |
| 화면 빌드 | `npm run build --prefix webui-vue` 통과. |
| 실제 기억 쓰기·읽기 | HTTP로 긴 한글 경험을 저장하고 3개 응답으로 나누어 읽어 원본 전체와 비교했다. 가설의 확인 방법, 실패의 이유·교훈도 저장 후 조회했다. |
| 실제 작업 쓰기·읽기 | 준비→시작→근거 저장→조건 확인→완료를 HTTP로 실행했다. 완료를 두 번 요청한 뒤 자동 결과 경험 1개와 작업 묶음의 완료 표시를 확인했다. |
| 큰 지도 | HTTP로 작업 30개를 만들고 2개 응답에서 중복 없이 모두 읽었다. 중간에 작업을 추가한 뒤 이전 조회 상태로 요청하면 거부됐다. 별도 검사에서는 종류별 항목 402개를 끝까지 읽었다. |
| 검색 데이터 관리 | 브라우저 버튼으로 실제 다시 시도·다시 만들기 POST 요청을 보냈다. 상태를 다시 조회했고, 로컬 시험용 검색 생성기로 경험 2개가 처리됐음을 DB에서 확인했다. 관리자 인증 없는 요청은 거부됐다. |
| Orca 화면 | 개요·계획·현황·보드·작업 흐름·Git·목표·기억·규칙 이동, 목록/지도 전환, 원문 열기, 상세 초점과 닫기를 확인했다. 393px 화면에서 페이지 가로 넘침이 없었다. |
| 다국어·화면 설정 | 한국어·영어로 11개 메뉴와 사실·가설 직접 주소, 오류 안내, 원본 기억 이동을 확인했다. 언어 변경 중 선택·원문·주소 유지와 추가 조회 없음, 새로고침 후 언어·화면 모드 유지, 두 언어의 393px 배치를 확인했다. 시험용 로그인 화면에서도 언어 변경·저장과 로그인 전 데이터 조회 없음이 확인됐다. |
| 9월 8일 작업 공간·상세 | 규칙·검색 관리 화면을 새로고침해 선택한 공간이 유지됨을 확인했다. HTTP로 같은 프로젝트의 두 작업을 만들고 서로 다른 담당 범위를 읽었으며, 다른 공간으로 조회하면 거부됐다. |
| 9월 8일 자동 갱신 | HTTP로 작업의 막힘을 해제·설정한 뒤 개요와 작업 현황에 자동 반영됐다. 선택한 상세, 검색어, 주소와 모바일 페이지 스크롤 300px가 유지됐다. 상위 묶음과 하위 작업의 같은 알림은 하나로 표시됐다. 브라우저에서 갱신 요청 실패를 재현해 보드 내용 유지와 다시 시도 후 복구도 확인했다. |
| 9월 8일 목록·폴더 쓰기 | 브라우저 요청에서 기억 목록을 먼저 읽고 선택 후 연결 정보를 읽는 순서를 확인했다. Git 등록 버튼으로 폴더를 저장하고 API 재조회·브라우저 새로고침에서 경로와 브랜치를 확인했다. |
| 9월 8일 모바일·다국어 | 393px에서 한국어·영어 개요, 현황, 계획, 보드, 작업 흐름, Git, 기억, 규칙 화면의 가로 넘침이 없었다. 폴더 등록의 필수 입력과 검색 결과 없음→조건 해제 동작을 확인했다. |
| 9월 8일 조회 순서 검사 | 브라우저에서 지도 HTTP 응답을 18초 늦춰 원문을 먼저 연 뒤에도 원본 링크가 유지됨을 확인했다. 기억 화면에서 관련 작업을 선택한 주소를 직접 열고 새로고침해 목적·담당 범위가 복원됨을 확인했다. |
| 커밋·원격 반영 | 앞선 개선은 `5c862b7dcd54dc2d02c76b963470bad32b6c6f43`로 반영했다. 9월 8일 추가 수정의 커밋·원격 반영은 미실행이다. 기존 README·인계 문서 변경과 미추적 파일은 보존했다. |
| 운영 빌드·배포 후 확인 | 미실행. 로컬 화면은 확인용 데이터다. |

## 발전시킬 만한 부분과 남은 한계

- **관련 기억의 순서:** 작업별 관련 기억은 표시한다. 실제 참조 기록이 쌓이면 결정·제약·실패 경험 중 어떤 것을 먼저 보여줄지 조정할 수 있다.
- **확인할 일의 범위:** 현재는 작업 상태와 목표 완료 여부를 다룬다. 오래된 계획 검토·확인 전 가설은 각 화면에서 확인한다. 실제 누락 사례가 생기면 같은 목록으로 확장할 수 있다.
- **큰 공간의 조회 비용:** 현재 지도는 페이지마다 전체 조회 결과를 만들고 변경 여부를 비교한다. 처리 시간이 실제로 길어지면 서버에서 일정 시점의 조회 결과를 유지하는 방식을 검토한다. 항목 한 개도 담지 못할 만큼 응답 제한이 작은 경우에는 원인을 표시한다.
- **작업 흐름 화면의 범위:** 작업 흐름 화면은 최대 200개 작업·400개 관계를 조회하고 더 있으면 일부 표시임을 알린다. 큰 공간에서는 전체 항목을 읽는 개요 목록을 사용할 수 있다.
- **로그인 유지 시간:** 실제 서버에서 세션 만료는 관찰했지만 운영 로그인 제공자의 설정은 확인하지 않았다. 로그인 후 이전 주소 복귀는 구현했으며, 운영 세션 갱신과 유지 시간은 별도 확인이 필요하다.

새 출처 조회용 DB 뷰는 마이그레이션 `00039_work_memory_context.sql`에 포함된다. 운영 반영 시 새 Go 서버 빌드와 해당 마이그레이션 적용이 필요하다.
